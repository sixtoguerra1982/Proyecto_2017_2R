$("#order_price2").text("Total: <%= number_to_currency(@total, separator: ',', delimiter: '.', precision: 0) %>");
$("#order_price").text("Total: <%= number_to_currency(@total, separator: ',', delimiter: '.', precision: 0) %>");
var noticia = "<%= @notice %>";
if (noticia != ""){
    alert("<%= @notice %>");
  }
